---
name: Bump Jenkins' LTS version in the controller Dockerfile

scms:
  default:
    kind: github
    spec:
      user: "{{ .github.user }}"
      email: "{{ .github.email }}"
      owner: "{{ .github.owner }}"
      repository: "{{ .github.repository }}"
      token: "{{ requiredEnv .github.token }}"
      username: "{{ .github.username }}"
      branch: "{{ .github.branch }}"

sources:
  JenkinsLatestLTS:
    name: Get the latest Jenkins LTS version
    kind: jenkins
    spec:
      release: lts

targets:
  setJenkinsLatestLTS:
    kind: dockerfile
    spec:
      file: jenkins/controller/Dockerfile
      instruction:
        keyword: "ARG"
        matcher: "JENKINS_VERSION"
    name: "[jenkins-controller] Bump Jenkins LTS version in jenkins/controller/Dockerfile"
    sourceid: JenkinsLatestLTS
    scmid: default

actions:
  default:
    kind: github/pullrequest
    scmid: default
    title: Update Jenkins LTS versions to {{ source "JenkinsLatestLTS" }} in the controller Dockerfile
    spec:
      labels:
        - dependencies
        - chore
